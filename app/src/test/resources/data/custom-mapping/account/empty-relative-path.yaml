version: v1.1
rules:
  - request:
      relative: true
    response:
      headers:
        content-type: application/json
      body: |
        {
          "id": "4",
          "name": "account4"
        }
